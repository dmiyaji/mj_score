USE mj_score;

CREATE TABLE IF NOT EXISTS seasons (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    is_active BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

INSERT INTO seasons (id, name, is_active, created_at, updated_at) 
VALUES ('00000000-0000-0000-0000-000000000001', 'Season 1', TRUE, NOW(), NOW());

ALTER TABLE game_results 
ADD COLUMN season_id VARCHAR(36) DEFAULT '00000000-0000-0000-0000-000000000001',
ADD COLUMN stage VARCHAR(20) DEFAULT 'REGULAR';

ALTER TABLE game_results 
ADD CONSTRAINT fk_gr_seasons FOREIGN KEY (season_id) REFERENCES seasons(id) ON DELETE RESTRICT;
